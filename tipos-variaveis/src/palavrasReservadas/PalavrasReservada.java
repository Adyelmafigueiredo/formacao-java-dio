package palavrasReservadas;

public class PalavrasReservada {
    /*
     * public: acesso por qualquer class
     * private: acesso apenas dentro da class
     * protected: acesso apenas por class do mesmo pacote
     */

     /*
      * modificadores de class=>
      *abstract: class que não pode ser instanciada ou método que precisa ser implementado
      * por uma class não abstrata.
      * extends: indica uma superclass que a subclass está estendendo.
      * final: impossibilita que uma class seja modificada o seu comportamento, que o método
      * seja sobescrito ou ou que uma váriavel seja reinicializada(constante).
      * implements: indica as interfaces que a class irá implementar.
      * 
      */
    
}
